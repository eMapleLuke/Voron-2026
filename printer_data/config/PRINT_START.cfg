#####################################################################
#   print_start macro for v2
#####################################################################

[gcode_macro PRINT_START]
gcode:
  # This part fetches data from your slicer. Such as bed, extruder, and chamber temps and size of your printer.
  {% set target_bed = params.BED|int %}
  {% set target_extruder = params.EXTRUDER|int %}
  {% set target_chamber = params.CHAMBER|default("45")|int %}
  {% set x_wait = printer.toolhead.axis_maximum.x|float / 2 %}
  {% set y_wait = printer.toolhead.axis_maximum.y|float / 2 %}
  {% set chamber_temp = printer['temperature_fan chamber'].temperature %}  # Chamber-Temperatur abrufen

#####################################################################
# 	Prep
#####################################################################

  # Home the printer, set absolute positioning and update the Stealthburner LEDs.
  STATUS_HOMING                                         # Set LEDs to homing-mode
  G28                                                   # Home X Y
  G90                                                   # Absolute position

  ##  Uncomment for bed mesh (1 of 2 for bed mesh)
  BED_MESH_CLEAR                                       # Clear old saved bed mesh (if any)
  G1 X{x_wait} Y{y_wait} F9000
  G1 Z50 F9000

#####################################################################
# 	High Temp Material
#####################################################################

  # Check if the bed temp is higher than 90c - if so then trigger a heatsoak.
  {% if params.BED|int > 90 %}

    #{% set chamber_temp = printer['temperature_fan chamber'].temperature %}  # Chamber-Temperatur abrufen
  
    {% if chamber_temp < 30 %}
      SET_DISPLAY_TEXT MSG="Bed: {target_bed}c"  
      STATUS_HEATING  
      M140 S{target_bed}
      SET_FAN_SPEED FAN=nevermore SPEED=1.0  # Nevermore Filter einschalten
      SET_TEMPERATURE_FAN_TARGET TEMPERATURE_FAN=chamber TARGET=50      
      M106 S255
      
      SET_DISPLAY_TEXT MSG="Soak for 30 min"
      G4 P1800000  # 30 Minuten warten (30 * 60 * 1000 ms)
      
      #SET_DISPLAY_TEXT MSG="Soak for 60 min"
      #G4 P3600000
      
      #SET_DISPLAY_TEXT MSG="Soak for 2h"
      #G4 P7200000
      
      M106 S0
    {% else %}
     SET_DISPLAY_TEXT MSG="Bed: {target_bed}c"  
     STATUS_HEATING  
     M140 S{target_bed}
     SET_DISPLAY_TEXT MSG="Chamber already warm, skipping heat soak"
     SET_TEMPERATURE_FAN_TARGET TEMPERATURE_FAN=chamber TARGET=50
     #SET_DISPLAY_TEXT MSG="Soak for 15 min"               # Display info on display
     G4 P900000
    {% endif %}

    #SET_DISPLAY_TEXT MSG="Heatsoak: {target_chamber}c"  # Display info on display
    #M141 S{CHAMBER}                                     # Set the target temp for the chamber
    SET_DISPLAY_TEXT MSG="Bed: {target_bed}c"  
    STATUS_HEATING                                                         
    #M106 S255                                           # Turn on the PT-fan
    G1 X{x_wait} Y{y_wait} F9000                    # Go to center of the bed
    #M190 S{target_bed}                                  # Set the target temp for the bed
    #SET_DISPLAY_TEXT MSG="Soak for 15 min"               # Display info on display
    #G4 P900000
    SET_DISPLAY_TEXT MSG="Hotend: 150c"
    M109 S150

#####################################################################
# 	Low Temp Material
#####################################################################

  # If the bed temp is not over 90c, then skip the heatsoak and just heat up to set temp with a 2 min soak
  {% else %}
    
    #SET_DISPLAY_TEXT MSG="Heatsoak: {target_chamber}c"  # Display info on display
    #M141 S{CHAMBER}                                     # Set the target temp for the chamber
    SET_TEMPERATURE_FAN_TARGET TEMPERATURE_FAN=chamber TARGET=10
    SET_DISPLAY_TEXT MSG="Bed: {target_bed}c"           # Display info on display
    STATUS_HEATING                                      # Set LEDs to heating-mode
    G1 X{x_wait} Y{y_wait} F9000                    # Go to center of the bed
    M190 S{target_bed}                                  # Set the target temp for the bed
    SET_DISPLAY_TEXT MSG="Soak for 15 min"               # Display info on display
    G4 P900000                                          # Wait 5 min for the bedtemp to stabilize
    SET_DISPLAY_TEXT MSG="Hotend: 150c"
    M109 S150
  {% endif %}

#####################################################################
# 	ABL
#####################################################################

  ##  Uncomment for V2.4 (Quad gantry level AKA QGL)
  SET_DISPLAY_TEXT MSG="Leveling"                      # Display info on display
  STATUS_LEVELING                                      # Set LEDs to leveling-mode
  G28 Z
  QUAD_GANTRY_LEVEL                                    # Level the printer via QGL
  G28 Z                                                # Home Z again after QGL

  ##  Uncomment for bed mesh (2 of 2 for bed mesh)
  SET_DISPLAY_TEXT MSG="Bed mesh"                      # Display info on display
  SET_DISPLAY_TEXT MSG="Cleaning Nozzle"               # Display info on display
  CLEAN_NOZZLE                                         # Clean the Nozzle for probing 
  CARTOGRAPHER_TOUCH_HOME                              # Home for real Z0
  STATUS_MESHING                                       # Set LEDs to bed mesh-mode
  BED_MESH_CALIBRATE ADAPTIVE=1                        # Start the bed mesh (add ADAPTIVE=1) for adaptive bed mesh
#####################################################################
# 	Head Hotend
#####################################################################

  # Heat up the hotend up to target via data from slicer
  SET_DISPLAY_TEXT MSG="Hotend: {target_extruder}c"     # Display info on display
  STATUS_HEATING                                        # Set LEDs to heating-mode
  G1 X15 Y306 Z5 F9000                                  # Move to the back to lead filament
  M107                                                  # Turn off partcooling fan
  M109 S{target_extruder}                               # Heat the hotend to set temp

#####################################################################
# 	Start Print
#####################################################################
 
  # Get ready to print by doing a primeline and updating the LEDs
  SET_DISPLAY_TEXT MSG="Printer goes brr"               # Display info on display
  STATUS_PRINTING                                       # Set LEDs to printing-mode
  #G1 Z5 F9000                                     # Move to the back to lead filament

  